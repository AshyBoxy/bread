export * as bible from "./bible";

import { Utils } from "../framework";
export const { randomNumber, randomFloat, randomInt, discord, mentions } = Utils;
export const random = randomNumber;
export { default as react } from "./react";
